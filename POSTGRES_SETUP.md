# Настройка PostgreSQL на Railway

Этот бот уже поддерживает PostgreSQL из коробки. Все что нужно - добавить базу данных на Railway.

## Шаг 1: Добавить PostgreSQL плагин на Railway

1. Откройте ваш проект на https://railway.app
2. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных и добавит переменную окружения `DATABASE_URL`

## Шаг 2: Проверить переменную DATABASE_URL

1. Перейдите в раздел **"Variables"** вашего сервиса
2. Убедитесь, что появилась переменная `DATABASE_URL` (Railway добавляет ее автоматически)
3. Формат должен быть: `postgresql://user:password@host:port/database`

**Важно**: Не удаляйте переменную `BOT_TOKEN` - она должна остаться!

## Шаг 3: Перезапустить сервис

1. Нажмите **"Deploy"** или просто сделайте новый git push
2. Бот автоматически определит PostgreSQL через `DATABASE_URL` и создаст все таблицы

## Что происходит автоматически?

При наличии `DATABASE_URL` бот:
- ✅ Подключается к PostgreSQL вместо SQLite
- ✅ Создает connection pool (5-20 соединений)
- ✅ Автоматически конвертирует все SQL-запросы в PostgreSQL формат
- ✅ Инициализирует все таблицы при первом запуске

## Проверка работы

После деплоя проверьте логи Railway:

```
✅ PostgreSQL connection pool инициализирован (5-20 соединений)
✅ Database initialized successfully
```

## Миграция данных из SQLite (опционально)

Если у вас были данные в SQLite (маловероятно, т.к. `/tmp/` очищается при деплое):

1. Скачайте SQLite файл с Railway (если он есть)
2. Используйте инструменты миграции:
   - `pgloader` - автоматическая миграция
   - Или вручную: экспортируйте данные и импортируйте в PostgreSQL

## Стоимость

Railway предоставляет:
- **$5 бесплатно каждый месяц**
- PostgreSQL обычно использует ~$0.50-2.00/месяц для малых проектов
- Это покрывается бесплатными кредитами

## Полезные команды Railway CLI (опционально)

```bash
# Установка CLI
npm i -g @railway/cli

# Подключение к PostgreSQL
railway connect postgres

# Просмотр переменных
railway variables

# Просмотр логов
railway logs
```

## Возврат к SQLite (для тестирования)

Если нужно временно вернуться к SQLite:

1. Удалите переменную `DATABASE_URL` в Railway Variables
2. Перезапустите сервис

**Внимание**: SQLite в `/tmp/` не сохраняет данные между деплоями!

## Поддержка

Код уже содержит полную поддержку PostgreSQL:
- `db.py:202` - автоматическое определение типа БД
- `db.py:382` - функция convert_sql для конвертации запросов
- `db.py:450` - init_db создает все таблицы

Никаких изменений в коде не требуется! 🎉
