# 🧹 Инструкция по очистке тестовых данных в Railway Database

## Что будет удалено

Этот скрипт удаляет все тестовые данные из старого бота по стройке:

### ❌ Тестовые кампании со старыми категориями:
- Электрика
- Сантехника
- Отделка
- Сборка мебели
- Окна/двери
- Бытовая техника
- Напольные покрытия
- Мелкий ремонт
- Дизайн

### ❌ Тестовые блогеры
- Все блогеры с Telegram ID от 100000000 до 100000999

### ❌ Тестовый рекламодатель
- "Тестовый клиент"

### ❌ Связанные данные
- Отклики на тестовые кампании
- Сообщения в чатах
- Категории кампаний и блогеров
- Настройки уведомлений
- Отказы от кампаний

---

## Способ 1: Через Railway Query Editor (Рекомендуется)

### Шаг 1: Откройте Railway Dashboard
1. Перейдите на [Railway.app](https://railway.app/)
2. Откройте проект `influencemarket-bot`
3. Перейдите в **PostgreSQL** (или вашу базу данных)

### Шаг 2: Откройте Query Editor
1. Нажмите на вкладку **Query** или **Data**
2. Вы увидите интерфейс для выполнения SQL запросов

### Шаг 3: Выполните SQL скрипт
1. Откройте файл `clean_test_data.sql`
2. Скопируйте весь содержимое
3. Вставьте в Query Editor
4. **ВАЖНО**: Сначала выполните только SELECT запросы (ШАГ 1) чтобы увидеть, сколько данных будет удалено
5. Если все правильно, выполните весь скрипт
6. В конце раскомментируйте `COMMIT;` чтобы подтвердить изменения

### Шаг 4: Проверьте результаты
SQL скрипт автоматически покажет статистику:
- Сколько кампаний осталось
- Сколько блогеров осталось
- Сколько откликов осталось

---

## Способ 2: Через Python скрипт на Railway

### Шаг 1: Подключитесь к Railway через SSH
```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
railway link
```

### Шаг 2: Запустите скрипт очистки
```bash
# Выполните скрипт на Railway
railway run python3 clean_test_data.py
```

### Шаг 3: Подтвердите удаление
Скрипт попросит подтверждение:
```
❓ Продолжить? (yes/no): yes
```

---

## Способ 3: Локальное подключение к Railway DB

### Шаг 1: Получите DATABASE_URL
1. В Railway Dashboard откройте ваш PostgreSQL сервис
2. Перейдите в **Variables**
3. Скопируйте значение `DATABASE_URL`

### Шаг 2: Установите переменную окружения
```bash
export DATABASE_URL="postgresql://postgres:..."
```

### Шаг 3: Запустите скрипт локально
```bash
python3 clean_test_data.py
```

---

## Проверка результатов

После очистки выполните эти запросы в Railway Query Editor:

### Проверить количество открытых кампаний:
```sql
SELECT COUNT(*) FROM campaigns WHERE status = 'open';
```

### Посмотреть оставшиеся категории кампаний:
```sql
SELECT DISTINCT category FROM campaigns;
```

### Проверить количество блогеров:
```sql
SELECT COUNT(*) FROM bloggers;
```

### Посмотреть всех рекламодателей:
```sql
SELECT id, name, city FROM advertisers;
```

---

## ⚠️ ВАЖНО: Безопасность

1. **Сделайте бэкап** перед выполнением скрипта!
   - В Railway: Database → Settings → Backups → Create Backup

2. **Проверьте данные** перед удалением:
   - Запустите только SELECT запросы из начала SQL скрипта
   - Убедитесь, что удаляются только тестовые данные

3. **Используйте транзакции**:
   - SQL скрипт использует `BEGIN` и `COMMIT`
   - Если что-то пойдет не так, можно выполнить `ROLLBACK`

---

## Откат изменений (если что-то пошло не так)

Если вы выполнили скрипт и поняли, что удалили не те данные:

### Вариант 1: Восстановление из бэкапа Railway
1. Перейдите в Railway Dashboard
2. Database → Settings → Backups
3. Найдите последний бэкап перед очисткой
4. Нажмите **Restore**

### Вариант 2: Отмена через ROLLBACK (если еще не сделали COMMIT)
```sql
ROLLBACK;
```

---

## Часто задаваемые вопросы

### Q: Могу ли я отменить удаление после выполнения скрипта?
**A:** Только если у вас есть бэкап базы данных. Операция DELETE необратима.

### Q: Удалятся ли реальные кампании блогеров?
**A:** Нет, скрипт удаляет только кампании со старыми категориями из строительного бота (Электрика, Сантехника и т.д.). Если у вас есть реальные кампании с этими категориями, их нужно переименовать перед очисткой.

### Q: Что если скрипт выдает ошибку "no such table"?
**A:** Это означает, что вы запускаете скрипт локально на пустой БД. Используйте Railway Query Editor или подключитесь к Railway БД через DATABASE_URL.

### Q: Сколько времени займет очистка?
**A:** Зависит от количества данных, обычно 1-5 секунд.

---

## Контакты

Если возникли проблемы или вопросы, создайте issue в репозитории.
